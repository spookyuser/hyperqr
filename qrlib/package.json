{
  "name": "qrlib",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "if [ -n \"$VERCEL\" ]; pnpm install -g wasm-pack; fi",
    "build": "test -d pkg && rm -rf pkg; wasm-pack build --target web --weak-refs --release",
    "postinstall": "pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}